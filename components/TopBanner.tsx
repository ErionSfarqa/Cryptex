"use client";

export default function TopBanner() {
  return (
    <div className="w-full bg-[var(--accent)]/15 text-[var(--text)] text-sm font-semibold tracking-wide px-4 py-2 text-center">
      Tip: Use stop-loss and take-profit orders to manage risk.
    </div>
  );
}
